name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

# Least-privilege: CI only needs to read the repository.
permissions:
  contents: read

# Cancel superseded runs on the same ref to save CI minutes.
concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  frontend:
    name: Frontend lint / type-check / test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: 22.x
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint (ESLint)
        run: npm run lint

      - name: Verify version sync (package.json / Cargo.toml / tauri.conf.json)
        run: |
          V=$(node -p "require('./package.json').version")
          C=$(grep '^version' src-tauri/Cargo.toml | head -1 | cut -d'"' -f2)
          T=$(grep '"version"' src-tauri/tauri.conf.json | head -1 | grep -oE '[0-9]+\.[0-9]+\.[0-9]+')
          echo "package.json=$V  Cargo.toml=$C  tauri.conf.json=$T"

          if [ "$V" = "$C" ] && [ "$V" = "$T" ]; then
            echo "Version sync OK: $V"
          else
            echo "::error::版本不一致: package=$V cargo=$C tauri=$T"
            exit 1
          fi

      - name: Run unit tests
        run: npm run test

      - name: Type-check and build frontend
        run: npm run build

  rust:
    name: Rust fmt / clippy / build / test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./src-tauri
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@4360b52568e2003a75bf9bc1d59f33a8e3fc893c
        with:
          components: clippy, rustfmt

      - name: Install Tauri Linux system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libgtk-3-dev libayatana-appindicator3-dev librsvg2-dev patchelf

      - name: Cache cargo
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            src-tauri/target
          key: ${{ runner.os }}-cargo-${{ hashFiles('src-tauri/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Check Rust formatting
        run: cargo fmt -- --check

      - name: Run clippy
        run: cargo clippy --all-targets -- -D warnings

      - name: Build Tauri binary (compile-only canary)
        run: cargo build

      - name: Run Rust tests
        run: cargo test

  actionlint:
    name: Lint workflow files
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Lint workflow files with actionlint
        uses: raven-actions/actionlint@3d39aea434753780c3b3d4a1a31c854b4dbf49d7  # v2.2.0
        with:
          version: v1.7.12
          fail-on-error: true

  # 双源契约校验（P1-3）：Rust timer.rs ⇄ TS types.ts 字段 + 色板一致性。
  # 任何一侧改字段/色板而不同步另一侧，此 job 直接 fail，阻断漂移。
  contract:
    name: Dual-source contract check (Rust ⇄ TS)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: 22.x

      - name: Verify Rust⇄TS contract (fields + color pool)
        run: node scripts/check-contract.mjs
